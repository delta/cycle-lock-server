import express, { Router } from 'express';
// import { userInfo } from '../controllers/Data/dashboardController';

const router: Router = express.Router();

// router.get('/', userInfo);

export default router;
